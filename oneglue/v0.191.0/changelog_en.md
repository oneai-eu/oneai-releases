# OneGlue v0.191.0 — What's New

**Release Date:** 2026-07-10

SCVMM: cleaner device telemetry, cloud→organization mapping, and a searchable VM fleet.

## Map private clouds to your customers' organizations

The Integrations → SCVMM page is redesigned around a cloud mapping matrix: every discovered private cloud in one place, each assigned to an organization inline — and you can assign many clouds to the same organization at once (multi-select bulk-assign). A discovery banner shows how many clouds are found, mapped, and still unmapped.

## Focused SCVMM device telemetry

The device page no longer dumps every cloud and every VM inline. The SCVMM telemetry card now shows a concise summary — provider status, total private-cloud and VM counts, and a compact per-cloud line (VMs, running/stopped) — so you reach the device's own details without endless scrolling.

## Searchable VM fleet view

A dedicated VM fleet page lists the full inventory — all clouds and their virtual machines — with search and filtering, for when you need the complete picture.

## Private clouds as assets

Each discovered private cloud now appears as a searchable "SCVMM Cloud" asset in its mapped organization, with capacity, usage and VM count — so clouds show up in global search and org asset lists.
