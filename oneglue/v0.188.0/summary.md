OneGlue v0.188.0 adds an AI device-analysis assistant that steps through diagnostics and produces a findings report, reorganizes the device page into a cleaner "Compute & Automation" panel, introduces favorites so frequently-used items sort to the top of every list, refreshes the remote-session connect dialog, adds keyboard navigation for search results, gives admins per-persona capability scopes (Observe / Diagnose / Remediate) and a workspace default AI model, and rounds out the release with SCVMM / Hyper-V inventory sync for MSP and multi-tenant environments.
