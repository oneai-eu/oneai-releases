OneGlue v0.196.0 lets you Start, Stop and Restart SCVMM virtual machines directly from the VM Fleet — each action confirmed and audit-logged, with graceful or hard modes and per-organization permissions. Plus a smoother fleet: the VM Fleet menu + dashboard tile appear instantly when clouds are mapped, whole-row click opens VM details, and removing an SCVMM server now does a clean, confirmed teardown.
